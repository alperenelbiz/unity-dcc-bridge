[project]
name = "dccbridge-tools"
version = "0.1.0"
description = "Generators and validators for the DCC Bridge Unity package."
requires-python = ">=3.11"
dependencies = ["pillow>=11.0", "numpy>=2.1"]

[tool.uv]
package = false
